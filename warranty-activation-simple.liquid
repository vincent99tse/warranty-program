{% comment %}
  Warranty Activation Form - Simplified Version
  This template uses a direct script and style tag approach
{% endcomment %}

<div id="warranty-activation-app"></div>

<!-- Make sure these filenames match exactly what's in your dist/assets folder -->
<script src="{{ 'index-CQp-3bsu.js' | asset_url }}"></script>
<link rel="stylesheet" href="{{ 'index-Dd1Hr2R2.css' | asset_url }}">

<style>
  /* Basic styling to ensure the app displays properly */
  #warranty-activation-app {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
    font-family: var(--font-body-family, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif);
  }
  
  /* Ensure the form inherits Kella theme styles */
  #warranty-activation-app .btn--primary {
    background-color: var(--color-button, #3b82f6);
    color: var(--color-button-text, white);
    border-radius: var(--buttons-radius-outset, 0.375rem);
    font-family: var(--font-button-family, inherit);
    font-weight: var(--font-button-weight, 500);
    font-style: var(--font-button-style, normal);
    padding: 0.75rem 1.5rem;
    min-width: 12rem;
    display: inline-block;
    text-align: center;
    cursor: pointer;
    border: none;
  }
  
  #warranty-activation-app .btn--secondary {
    background-color: transparent;
    color: var(--color-button, #3b82f6);
    border: 1px solid var(--color-button, #3b82f6);
    border-radius: var(--buttons-radius-outset, 0.375rem);
    font-family: var(--font-button-family, inherit);
    font-weight: var(--font-button-weight, 500);
    font-style: var(--font-button-style, normal);
    padding: 0.75rem 1.5rem;
    min-width: 12rem;
    display: inline-block;
    text-align: center;
    cursor: pointer;
  }
  
  #warranty-activation-app .field__input,
  #warranty-activation-app .select__select {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #d1d5db;
    border-radius: 0.375rem;
    font-size: 1rem;
  }
  
  #warranty-activation-app .form__label {
    display: block;
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: #374151;
  }
  
  /* Fix layout issues */
  .page-width {
    max-width: 100% !important;
  }
  
  /* Fix progress bar */
  #warranty-activation-app .progress-steps {
    overflow: visible;
    display: flex;
    justify-content: space-between;
    position: relative;
    margin-bottom: 1rem;
  }
  
  #warranty-activation-app .progress-steps::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    height: 2px;
    background-color: #e5e7eb;
    transform: translateY(-50%);
    z-index: 0;
  }
  
  #warranty-activation-app .progress-step {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  #warranty-activation-app .step-number {
    width: 2rem;
    height: 2rem;
    border-radius: 9999px;
    background-color: #e5e7eb;
    color: #6b7280;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  
  #warranty-activation-app .progress-step.active .step-number {
    background-color: var(--color-primary, #4f46e5);
    color: white;
  }
  
  #warranty-activation-app .progress-step.completed .step-number {
    background-color: #10b981;
    color: white;
  }
  
  /* Fix grid layout */
  @media (min-width: 768px) {
    #warranty-activation-app .grid-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }
    
    #warranty-activation-app .info-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
    }
    
    #warranty-activation-app .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }
    
    #warranty-activation-app .form-grid.three-columns {
      grid-template-columns: 1fr 1fr 1fr;
    }
  }
</style>